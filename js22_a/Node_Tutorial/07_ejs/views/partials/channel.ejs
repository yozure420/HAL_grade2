<div
    class="group relative bg-white rounded-none shadow-sm hover:shadow-lg transition-all duration-200 cursor-pointer border border-transparent hover:border-slate-100">
    <!-- Image -->
    <div class="aspect-w-1 aspect-h-1 w-full overflow-hidden bg-gray-200 relative">
        <!-- Like Button -->
        <button
            class="like-button absolute top-2 right-2 p-2 rounded-full bg-white/80 hover:bg-white transition-colors z-10"
            data-like-type="channel" data-id="<%= channel.id %>">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-gray-400 transition-colors" fill="none"
                viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
            </svg>
        </button>

        <a href="/channels/<%= channel.id %>">
            <!-- TODO: 
                1. src に画像パスを設定: channel.image
                2. alt にチャンネル名を設定: channel.name
                    ejsesc スニペットを活用
            -->
            <img src="<%= channel.image %>" alt="<%= channel.name %>"
                class="h-full w-full object-cover object-center group-hover:opacity-75 transition-opacity duration-300">
        </a>

        <% if (channel.status==='coming_soon' ) { %>
        <div class="absolute top-0 left-0 bg-sky-500 text-white text-xs font-bold px-3 py-1">
            COMING SOON
        </div>
        <% } else if (channel.status==='on_air' ) { %>
        <div class="absolute top-0 left-0 bg-pink-600 text-white text-xs font-bold px-3 py-1">
            ON AIR
        </div>
        <% } %>
    </div>

    <!-- Content -->
    <!-- TODO: /channels/:id にリンク -->
    <a href="">
        <div class="mt-4 px-2 pb-4">
            <!-- TODO: チャンネル名: channel.name: ejsout スニペットを活用 -->
            <h3 class="text-md font-bold text-gray-700 line-clamp-2 min-h-[40px] group-hover:text-sky-600 transition-colors">
                <%= channel.name %>
            </h3>
            <!-- TODO: キャスト: channel.cast: ejsout スニペットを活用 -->
            <p class="mt-1 py-2 text-xs text-gray-900">
                <%= channel.cast %>
            </p>
            <!-- TODO: 説明: channel.description: ejsout スニペットを活用 -->
            <p class="mt-1 text-sm text-gray-500 line-clamp-1">
                <%= channel.description %>
            </p>
        </div>
    </a>
</div>