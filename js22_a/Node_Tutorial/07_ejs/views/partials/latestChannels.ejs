<section class="space-y-8">
    <h2 class="text-center text-3xl text-red-500 font-bold">Now On Air</h2>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-y-10">
        <!-- 繰り返し -->
        <% channels.forEach(channel=> { %>
        <%- include('channel', { channel }) %>
        <% }) %>
    </div>
</section>